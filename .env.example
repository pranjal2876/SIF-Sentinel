# ===================================================
# SIF Sentinel — Environment Configuration
# ===================================================

# Frontend (Next.js)
NEXT_PUBLIC_API_URL=https://sif-sentinel-backend.onrender.com/api/v1

# Backend (FastAPI / Render)
PORT=8000
CORS_ORIGINS=*
JWT_SECRET=sif-sentinel-secure-production-secret-replace-me

# Optional LLM Integration (Defaults to deterministic rule-based ontology)
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
# GEMINI_API_KEY=
